Steps to run locally:

copy repository locally
ensure you have dependencies installed (express, etc.)
run "npm run devstart" - this will immediately re-run the server called in app.js each time you make a change 
should be available on localhost:3000
master branch is called master 

